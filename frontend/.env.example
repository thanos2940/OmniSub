# Override the backend API base URL for the built frontend.
# Leave unset to use the same origin (reverse-proxy friendly) in production,
# or http://localhost:8000 in dev.
# VITE_API_URL=http://192.168.1.50:8000
